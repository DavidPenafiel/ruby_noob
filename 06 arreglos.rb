# a = Array.new #arreglo vacío
# a = Array.new(10) #para 10 espacios
# a = Array.new(5, 1) #para 5 espacios con valor 1
# a = Array(1..10) #10 valores del 1 al 10
# a = [1,9,8,4] #valores personalizados
# a.push(5) #agrega el valor 5 al final 1,9,8,4,5
# a.unshift(0) #agrega el valor 0 al inicio
# a.insert(3,2) # inserta el valor 2 en la cuarta posición ya que la primera posición es 0
# a.shift # elimina el último elemento pero primero lo devuelve o presenta en pantalla
# a.shift # elimina el primer elemento pero primero lo devuelve o presenta en pantalla
# a.delete_at(3) # elimina el elemento en el índice 4 pero primero lo presenta
# a.delete(3) # elimina todos los elementos que sean "3"
# a.uniq # presenta el arreglo eliminando elementos repetidos pero no
# a.uniq! # elimina permantentemente del arreglo los elementos repetidos de este
# a << 11 es como hacer un push(11) al final
# a[3] # presenta el tercer número del arreglo
# a[-2] # presenta el penúltimo número del arreglo
# a[0..2] # presenta los tres primeros números del arreglo
# a.first # presenta el primer elemento
# a.last # presenta el último elemento
# a.take(2) # presenta los dos primeros elementos
# a.drop(2) # presenta todo menos los dos primeros elementos
